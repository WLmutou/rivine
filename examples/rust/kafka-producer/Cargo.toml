[package]
name = "kafka-producer"
version = "0.1.0"
edition = "2024"

[dependencies]
rdkafka = { version = "0.31", features = ["tokio"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }